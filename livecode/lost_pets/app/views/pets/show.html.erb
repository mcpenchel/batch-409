<%= link_to "Back to main page", pets_path, class: 'btn btn-primary' %>

<h1><%= @pet.name %></h1>
<h2><%= @pet.species %></h2>
<h3><%= @pet.rescued_at %></h3>
<h3><%= @pet.gender %></h3>

<%= link_to "Adopted!", pet_path(@pet), method: 'DELETE', data: { confirm: "Are you sure it was 'adopted'?" }, class: 'btn btn-danger' %>

<%= link_to "Edit #{@pet.name}", edit_pet_path(@pet), class: 'btn btn-warning' %>
